<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:map="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps"
             xmlns:local="clr-namespace:MoodFull.CustomizedMap"
             mc:Ignorable="d"
             x:Class="MoodFull.Views.MapPage">
    <ContentPage.Content>
        <Grid>
            <local:CustomMap MapType="Street" HasZoomEnabled="True">
                <local:CustomMap.ItemTemplate>
                    <DataTemplate>
                        <local:CustomPin Position="{Binding Position}"
                                         Address="{Binding Address}"
                                         Label="{Binding Label}"/>
                    </DataTemplate>
                </local:CustomMap.ItemTemplate>
            </local:CustomMap>
            <ContentView Content="{Binding RestMap}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>